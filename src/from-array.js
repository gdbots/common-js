'use strict';

export default class FromArray
{
  /**
   * Returns a new object from the provided array.  The array
   * should be data returned from toArray or at least match
   * that signature.
   *
   * @param array data
   *
   * @return array
   */
  static fromArray(data = {}) {
    throw new Error('Interface function.');
  }
}
